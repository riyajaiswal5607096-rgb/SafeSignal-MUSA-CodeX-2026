# SafeSignal — MUSA CodeX 2026

Recreated from the submitted SafeSignal MVP demonstration video and the provided Flask `app.py`.

## MVP flow

Quick Report → Pattern Map → Why Flagged → Authority Alert

The demo uses synthetic demonstration data.

## Run locally

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

Then open:

http://127.0.0.1:5000
